apply plugin: 'groovy'
apply plugin: 'maven'

sourceCompatibility = '1.8'

group = 'com.devbliss.doctest'
version = '0.1.0'

repositories {
  mavenLocal()
  mavenCentral()
}

dependencies {
  compile gradleApi()
  compile localGroovy()
  
  testCompile('com.devbliss.doctest:doctest:0.8.0')
}
